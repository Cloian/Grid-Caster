# AGENTS.md

## 프로젝트 개요

이 프로젝트는 Unity 2D와 C#으로 제작하는 타일맵 기반 전략 턴제 웨이브 생존 게임이다.

게임은 포켓몬 불가사의 던전처럼 타일 단위로 이동하며, 체스처럼 플레이어가 한 번 행동한 뒤 맵의 모든 적과 보드에 남는 적·특수 투사체가 각각 한 번씩 행동한다.

현재 목표는 직업, 동료, 스토리, 최종보스가 포함된 완성형 RPG가 아니다.

먼저 다음 핵심 반복 구조를 가진 플레이 가능한 프로토타입을 완성한다.

1. 플레이어가 이동 또는 공격 중 하나를 선택한다.
2. 플레이어 기본공격 투사체는 같은 플레이어 행동 안에서 전체 경로와 피해를 처리한다.
3. 플레이어 행동이 완료되면 모든 적이 각각 한 번 행동한다.
4. 보드에 남는 적·특수 투사체가 각각 한 번 이동한다.
5. 충돌, 피해, 사망을 판정한다.
6. 웨이브의 모든 적을 처치하면 강화 하나를 선택한다.
7. 다음 웨이브를 진행한다.
8. 플레이어가 사망할 때까지 반복한다.

---

## 기술 환경

* 엔진: Unity 2D
* 현재 Unity 버전: `6000.4.0f1`
* 언어: C#
* 시점: 탑다운
* 그래픽: 2D 픽셀 아트
* 맵 방식: Tilemap 및 정수형 타일 좌표
* 기본 테스트 맵: 12×12
* 플랫폼: PC
* 입력 방식: 키보드 우선
* 렌더링: Universal Render Pipeline 2D
* 입력 패키지: Unity Input System
* 스프라이트 기준: 32 PPU

Unity 버전과 현재 프로젝트 설정을 먼저 확인한 뒤 코드를 작성한다.

프로젝트에 이미 설치된 패키지와 입력 시스템을 확인하지 않고 새로운 패키지를 임의로 추가하지 않는다.

---

## 현재 프로젝트 상태와 전환 원칙

현재 기준 씬은 `Assets/Scenes/SampleScene.unity`이며 Build Settings에도 이 씬이 등록되어 있다.

현재 디스크에 저장된 프로젝트 상태는 다음과 같다.

* `SampleScene`의 Tilemap 저장 범위는 현재 16×15이다.
* 설계상 기본 테스트 맵 목표는 12×12이므로 현재 씬 크기와 목표 크기를 혼동하지 않는다.
* 활성 플레이어 GameObject 이름은 `player`이다.
* 플레이어에는 `Move`, `SpriteRenderer`, `Animator`가 연결되어 있다.
* 플레이어 Animator Controller는 `Assets/Player/idle-run.controller`이다.
* 플레이어 애니메이션은 bool 파라미터 `IsMoving`으로 idle과 walk를 전환한다.
* `CameraController`는 플레이어 추적, Y 키 고정 전환, 마우스 자유 이동과 줌을 담당한다.
* `Move`, `MonsterMovement`, `MonsterSpawner`에는 이전 실시간 프로토타입 구현이 남아 있다.
* `Assets/Monsters/Prefabs/`에는 이전 구현에서 만든 Bat와 Snake Prefab이 남아 있다.

현재 남아 있는 실시간 적 이동, 시간 간격 스폰, 몬스터 Prefab 기반 구조는 최종 설계 기준이 아니라 전환 대상인 이전 구현이다.

새 기능은 이 문서의 턴제 흐름을 기준으로 작성한다.

기존 구현을 전환할 때는 다음 원칙을 지킨다.

* 플레이어 이동을 포함한 게임 규칙은 점진적으로 `TurnManager`, `GridManager`, 타일 좌표 기반 구조로 옮긴다.
* 적을 `Update()`에서 계속 이동시키는 코드를 새로 추가하지 않는다.
* 기존 실시간 코드를 한 번에 전부 재작성하지 않고 사용자가 요청한 단계에 필요한 범위만 전환한다.
* 현재 코드와 새 규칙이 충돌하면 새 규칙을 우선하되, 변경되는 기존 동작을 완료 보고에 명시한다.
* 카메라 보간, 애니메이션 재생, 타일 이동 연출 같은 시각 표현은 `Update()` 또는 `LateUpdate()`를 사용할 수 있다.
* 시각 표현이 턴 판정, 점유, 피해 또는 행동 횟수를 결정하게 만들지 않는다.

---

## 프로젝트 폴더와 에셋 규칙

주요 경로의 역할은 다음과 같다.

* `Assets/Codes/`: 런타임 C# 스크립트
* `Assets/Editor/`: Unity Editor 전용 도구
* `Assets/Player/`: 플레이어 스프라이트, AnimationClip, Animator Controller
* `Assets/Monsters/`: 몬스터 Aseprite 원본과 독립 애니메이션 및 설정 에셋
* `Assets/Scenes/`: 게임 씬
* `Assets/TIles/`: Tilemap과 Tile Palette 에셋. 현재 폴더명의 대소문자와 철자를 그대로 유지한다.
* `Assets/Settings/`: URP 및 렌더러 설정
* `Packages/`: Unity 패키지 의존성
* `ProjectSettings/`: Unity 프로젝트 설정

다음 폴더는 Unity 또는 IDE가 생성하므로 직접 수정하거나 결과물로 취급하지 않는다.

* `Library/`
* `Temp/`
* `Logs/`
* `obj/`
* `.vs/`

에셋 관리 시 다음 규칙을 지킨다.

* 게임에서 사용하는 모든 Sprite와 Aseprite 에셋은 32 PPU로 고정한다.
* 픽셀 아트는 Point 필터링을 사용하고 픽셀 경계를 흐리게 만드는 설정을 피한다.
* Aseprite 원본 파일은 그대로 보관한다.
* 몬스터에 대해서는 Aseprite Importer의 Model Prefab 자동 생성을 사용하지 않는다.
* 몬스터를 별도 `.prefab` 파일로 만들지 않는다.
* 몬스터 AnimationClip은 독립 `.anim` 파일로 저장한다.
* 몬스터 Animator Controller는 독립 `.controller` 파일로 저장한다.
* 권장 보관 위치는 `Assets/Monsters/Animations/<MonsterName>/`과 `Assets/Monsters/Controllers/`이다.
* Aseprite 내부 sub-asset만을 최종 AnimationClip이나 Animator Controller 참조로 사용하지 않는다.
* 스포너는 독립 Sprite와 Animator Controller 또는 몬스터 설정 데이터를 이용해 런타임 GameObject를 구성한다.
* 기존 `Assets/Monsters/Prefabs/` 파일은 이전 구현으로 취급한다. 별도 요청 없이 삭제하지 않지만 새 기능에서 사용하거나 다시 생성하지 않는다.
* 에셋을 이동하거나 이름을 바꿀 때 `.meta` 파일을 함께 유지해 GUID 참조를 보존한다.
* `.meta` 파일을 임의로 삭제하거나 새 GUID로 교체하지 않는다.
* 씬, Animator Controller, AnimationClip 같은 Unity YAML 파일은 가능하면 Unity Editor 또는 `Assets/Editor/` 도구로 수정한다.
* Unity YAML을 직접 수정해야 한다면 GUID, fileID, 컴포넌트 참조를 확인하고 변경 범위를 최소화한다.

현재 몬스터 원본 경로는 다음과 같다.

* Bat: `Assets/Monsters/Bat/bat_frame.aseprite`
* Snake: `Assets/Monsters/Snake/snake_asprite.aseprite`

---

## 핵심 게임 규칙

### 시간 진행

플레이어가 행동할 때만 게임 시간이 진행된다.

플레이어 입력과 그 입력으로 시작된 행동 연출이 없으면 다음 요소는 모두 정지해야 한다.

* 적
* 보드에 남는 플레이어 특수 투사체
* 적 투사체
* 함정
* 상태 효과
* 기타 턴 기반 오브젝트

적과 보드에 남는 투사체의 게임 규칙을 `Update()`에서 지속적으로 진행시키지 않는다.

플레이어 기본공격 투사체는 발사 순간 타일 경로와 충돌 대상을 확정하고, 같은 행동 안에서 `Update()`를 사용해 빠른 비행 연출만 보여줄 수 있다.

모든 턴 진행은 `TurnManager`가 명시적으로 호출한다.

### 플레이어 행동

플레이어는 한 턴에 다음 행동 중 하나만 수행할 수 있다.

* 한 칸 이동
* 기본 공격
* 원거리 공격
* 이동 스킬
* 필살기
* 대기

이동과 공격을 같은 턴에 동시에 수행할 수 없다.

### 기본 턴 순서

현재 턴 순서는 다음과 같다.

1. 플레이어 행동
2. 플레이어 기본공격이라면 전체 경로 비행, 충돌과 피해 처리
3. 적 행동
4. 보드에 남는 적·특수 투사체 행동
5. 함정과 상태 효과 처리
6. 충돌과 사망 판정
7. 웨이브 종료 판정
8. 다음 플레이어 입력 대기

동료 시스템을 나중에 추가할 경우 플레이어 행동과 적 행동 사이에 동료 행동 단계를 삽입할 수 있도록 설계한다.

현재는 동료 턴을 구현하지 않는다.

---

## 타일과 좌표 규칙

모든 게임 규칙은 월드 좌표가 아닌 정수형 타일 좌표를 기준으로 처리한다.

권장 좌표 형식:

```csharp
Vector2Int gridPosition;
```

거리, 사거리, 이동 가능 여부, 공격 가능 여부는 타일 좌표로 계산한다.

초기 이동 방향은 다음 네 방향만 사용한다.

* 위
* 아래
* 왼쪽
* 오른쪽

초기 버전에서는 대각선 이동과 대각선 투사체를 구현하지 않는다.

기본 거리 계산은 맨해튼 거리를 사용한다.

```csharp
int distance =
    Mathf.Abs(target.x - current.x) +
    Mathf.Abs(target.y - current.y);
```

캐릭터의 실제 Transform 이동은 타일 좌표가 확정된 후 시각적으로 반영한다.

게임 규칙이 Transform의 부동소수점 위치에 직접 의존하지 않도록 한다.

---

## 타일 점유 규칙

`GridManager`가 타일의 이동 가능 여부와 점유 상태를 관리한다.

다음 규칙을 지킨다.

* 벽이 있는 타일로 이동할 수 없다.
* 장애물이 있는 타일로 이동할 수 없다.
* 다른 캐릭터가 있는 타일로 이동할 수 없다.
* 플레이어와 적은 같은 타일을 점유할 수 없다.
* 적끼리 같은 타일을 점유할 수 없다.
* 사망한 캐릭터는 즉시 점유 정보를 해제한다.
* 먼저 행동한 적의 이동 결과를 다음 적이 확인해야 한다.
* 이동 전에 목적지 타일을 검사한다.
* 이동 성공 후 기존 타일과 새 타일의 점유 정보를 갱신한다.

투사체는 캐릭터와 별도의 점유 방식으로 관리할 수 있다.

---

## 투사체 규칙

플레이어 기본공격 투사체와 보드에 남는 적·특수 투사체는 서로 다른 이동 규칙을 사용한다.

모든 투사체는 발사 순간 다음 정보가 결정된다.

* 소유자
* 현재 타일 좌표
* 이동 방향
* 피해량
* 관통 가능 횟수

일반 투사체는 발사 후 플레이어나 목표물의 위치를 추적하지 않는다.

### 플레이어 기본공격 투사체

* 플레이어 행동 한 번 안에서 첫 유효 대상 또는 맵 끝의 경계 벽까지 전체 경로를 이동한다.
* 기본공격에는 사거리 제한을 두지 않는다.
* 경계 벽 타일까지 비행한 뒤 벽에 닿으면 제거한다.
* 발사 순간 타일 경로와 충돌 대상을 확정한다.
* 비행 연출이 끝나고 피해가 처리된 뒤에만 적 턴을 시작한다.
* 적중이 확정되면 투사체를 즉시 비활성화하고 제거하며, 피해 대상을 관통해 계속 표시하지 않는다.
* Transform 이동과 프레임 애니메이션은 실시간으로 표현할 수 있지만 판정은 연출 속도에 의존하지 않는다.
* 기본공격 투사체는 다음 턴까지 보드에 남지 않는다.

### 적·특수 투사체

* 보드에 남으며 매 투사체 턴마다 정해진 방향으로 한 칸 이동한다.
* 플레이어 입력이 없으면 이동하지 않는다.

다음 상황에서는 제거한다.

* 벽과 충돌
* 장애물과 충돌
* 유효한 대상과 충돌
* 맵 바깥으로 이동

유도탄은 현재 구현하지 않는다. 핵심 전투, 웨이브, 성장과 나머지 게임 구성이 모두 완성된 뒤에만 별도의 특수공격 후보로 다시 검토한다.

---

## 적 행동 규칙

모든 적은 플레이어 행동 이후 각각 한 번만 행동한다.

초기 적 유형은 다음과 같다.

### 근접 추적형

* 플레이어와 인접하면 공격한다.
* 인접하지 않으면 플레이어 방향으로 한 칸 이동한다.

### 원거리형

* 플레이어가 사거리 안에 있고 직선상에 있으면 투사체를 발사한다.
* 너무 멀면 접근한다.
* 너무 가까우면 후퇴할 수 있다.

### 랜덤 이동형

* 지정된 활동 범위 안에서 무작위 방향으로 한 칸 이동한다.
* 막힌 타일은 선택하지 않는다.
* 플레이어가 감지 범위에 들어오면 추적 행동으로 전환할 수 있다.

초기에는 단순한 축 우선 이동을 사용한다.

복잡한 길 찾기가 필요해질 때만 BFS 또는 A*를 추가한다.

---

## 웨이브 규칙

한 맵에서 여러 웨이브가 진행된다.

웨이브 기본 흐름:

1. 적 생성
2. 전투 진행
3. 모든 적 처치 확인
4. 강화 선택지 표시
5. 강화 하나 적용
6. 다음 웨이브 시작

웨이브가 증가할수록 다음 요소를 조절할 수 있다.

* 적 수
* 적 체력
* 적 공격력
* 적 유형
* 원거리 적 비율
* 적 생성 위치

현재 게임의 기본 목표는 최종보스 처치가 아니라 최대한 오래 생존하는 것이다.

도달한 최고 웨이브 기록을 저장할 수 있도록 설계한다.

---

## 성장과 특성

웨이브 종료 후 무작위 강화 선택지 세 개를 표시하고 하나를 선택한다.

초기 강화 예시:

* 공격력 증가
* 최대 체력 증가
* 현재 체력 회복
* 방어력 증가
* 투사체 피해 증가
* 투사체 관통 증가
* 필살기 게이지 획득량 증가
* 대시 거리 증가

초기 특성 예시:

* 추가 평타
* 고정 추가 피해
* 1타 2피
* 관통 공격
* 연쇄 공격
* 처치 시 회복
* 게이지 추가 충전
* 밀쳐내기

현재 프로토타입의 시작 특성 네 개는 다음 규칙으로 고정한다.

* 한 플레이에서는 시작 시 선택한 특성 하나만 활성화한다.
* 더블 캐스트는 공격 5회 셔플 백마다 한 번 발동하며 같은 방향으로 기본공격을 한 번 더 발사한다.
* 관통은 공격 3회 셔플 백마다 한 번 발동하며 첫 대상 이후 다음 대상 한 명까지 공격한다.
* 데미지 강화는 공격 4회 셔플 백마다 한 번 발동하며 해당 기본공격 피해를 1 증가시킨다.
* 넉백은 공격 4회 셔플 백마다 한 번 발동하며 살아남은 대상을 공격 방향의 빈 타일로 한 칸 이동시킨다.
* 더블 캐스트의 추가 공격은 특성을 다시 추첨하거나 재귀적으로 추가 공격을 만들지 않는다.
* 더블 캐스트나 관통으로 여러 번 적중해도 필살기 게이지는 플레이어 행동 한 번당 한 번만 획득한다.
* 넉백 목적지가 벽, 맵 바깥, 플레이어 또는 다른 적으로 막히면 피해만 적용하고 이동시키지 않는다.

강화와 특성 데이터는 가능하면 `ScriptableObject`로 관리한다.

각 데이터에는 최소한 다음 필드를 둔다.

* 고유 ID
* 표시 이름
* 설명
* 효과 종류
* 효과 수치
* 중첩 가능 여부
* 최대 중첩 수
* 희귀도 또는 등장 가중치

---

## 현재 구현 범위

현재 우선순위는 다음과 같다.

### 1단계: 기본 타일 이동

* 12×12 테스트 맵
* 플레이어 상하좌우 이동
* 타일 좌표 관리
* 벽 충돌
* 타일 점유
* 플레이어 입력이 있을 때만 행동

### 2단계: 기본 턴 전투

* 근접 적 한 종류
* 플레이어 행동 후 적 행동
* 플레이어 기본 공격
* 적 기본 공격
* 체력
* 피해
* 사망
* 게임 오버

### 3단계: 투사체

* 플레이어 원거리 공격
* 원거리 적
* 플레이어 기본공격 투사체의 같은 행동 내 전체 경로 처리
* 적 투사체의 턴당 한 칸 이동
* 벽 충돌
* 캐릭터 충돌
* 투사체 제거

### 4단계: 적 행동 확장

* 랜덤 이동 적
* 거리 유지형 원거리 적
* 돌진 예고 적
* 고정 포탑 적

### 5단계: 웨이브

* 웨이브별 적 생성
* 모든 적 처치 판정
* 다음 웨이브
* 난이도 증가
* 최고 기록

### 6단계: 성장

* 웨이브 종료 보상
* 무작위 강화 세 개
* 강화 선택
* 특성 적용
* 특성 중첩

### 7단계: 스킬과 필살기

* 대시
* 필살기 게이지
* 범위 공격
* 관련 UI

한 번에 여러 단계를 구현하지 않는다.

사용자가 특정 단계를 요청하면 해당 단계에 필요한 최소 기능만 구현한다.

---

## 추후 개선 사항

다음 요소는 핵심 전투와 웨이브 시스템이 안정적으로 완성된 후 추가한다.

현재 단계에서는 구현하지 않는다.

### 직업 시스템

추후 추가 가능한 직업:

* 기사
* 궁수
* 마법사
* 암살자

직업마다 다음 항목을 다르게 설정할 수 있다.

* 기본 체력
* 공격력
* 방어력
* 사거리
* 시작 스킬
* 필살기
* 고유 특성

현재는 하나의 기본 플레이어 캐릭터만 사용한다.

직업 선택 화면이나 직업별 코드를 미리 만들지 않는다.

### 동료 시스템

추후 플레이어가 전투나 탐험 중 동료를 모집하는 시스템을 추가할 수 있다.

예상 턴 순서:

1. 플레이어 행동
2. 동료 행동
3. 적 행동
4. 투사체 행동
5. 턴 종료

현재는 다음 항목을 구현하지 않는다.

* 동료 클래스
* 동료 AI
* 동료 모집
* 동료 성장
* 동료 전용 UI

단, `TurnManager`는 동료 턴을 나중에 삽입할 수 있을 정도로 책임을 분리한다.

### 최종보스

최종보스는 일반 적과 웨이브 밸런스가 완성된 후 추가한다.

추후 보스에 사용할 수 있는 기능:

* 공격 예고
* 여러 페이즈
* 적 소환
* 직선 공격
* 범위 공격
* 돌진
* 위험 타일 생성
* 체력에 따른 패턴 변화

현재는 보스 클래스, 보스 데이터, 엔딩 시스템을 구현하지 않는다.

### 기타 추후 콘텐츠

* 여러 맵을 연결한 탐험
* 랜덤 맵 생성
* 상점
* 이벤트 방
* 보물방
* 상태 이상
* 정예 적
* 도감
* 업적
* 영구 성장
* 캐릭터 해금
* 무한 모드
* 난이도 선택

---

## 권장 클래스 책임

### GameManager

* 게임 전체 상태
* 게임 시작
* 게임 오버
* 재시작
* 기록 관리

### TurnManager

* 현재 턴 상태
* 플레이어 입력 허용 여부
* 플레이어 행동 완료 처리
* 적 행동 순차 실행
* 투사체 행동 순차 실행
* 턴 종료 처리

`TurnManager`에 전투 계산, 이동 가능 여부, UI 처리까지 몰아넣지 않는다.

### GridManager

* 타일 좌표와 월드 좌표 변환
* 이동 가능 여부
* 타일 점유 등록과 해제
* 맵 범위 확인

### GridEntity

* 현재 타일 좌표
* 타일 위치 변경
* 월드 위치 반영
* 공통 점유 처리

### CharacterBase

* 최대 체력
* 현재 체력
* 공격력
* 방어력
* 피해 처리
* 사망 처리

### PlayerController

* 플레이어 입력
* 행동 선택
* 이동 요청
* 공격 요청
* TurnManager에 행동 완료 전달

### EnemyBase

* 적 공통 능력치
* 적 턴 행동
* 이동 또는 공격 결정
* 사망 처리

### Projectile

* 현재 타일 좌표
* 이동 방향
* 피해량
* 관통 횟수
* 턴 단위 이동
* 충돌 처리

### CombatManager

* 공격 판정
* 피해 계산
* 공격 대상 확인
* 사망 요청

### WaveManager

* 웨이브 번호
* 적 생성
* 생존 적 수 확인
* 웨이브 종료
* 다음 웨이브 시작

### TraitSystem

* 보유 특성
* 특성 적용
* 중첩 처리
* 공격 이벤트와 처치 이벤트 연결

---

## 코딩 규칙

* 스크립트 파일 이름과 클래스 이름을 동일하게 작성한다.
* 클래스마다 책임을 하나의 주제로 제한한다.
* 지나치게 복잡한 디자인 패턴을 사용하지 않는다.
* 초보자가 이해할 수 있는 구조를 우선한다.
* 핵심 로직에는 한국어 주석을 작성한다.
* 변수와 메서드 이름은 의미가 명확한 영어를 사용한다.
* 들여쓰기는 공백 4칸을 사용한다.
* 타입과 메서드는 PascalCase를 사용한다.
* private 필드와 지역 변수는 camelCase를 사용한다.
* 매직 넘버 대신 상수, 설정값 또는 ScriptableObject 데이터를 사용한다.
* Inspector에서 설정할 값에는 public 필드 대신 `[SerializeField] private`을 우선 사용한다.
* 불필요하게 모든 필드를 `public`으로 만들지 않는다.
* 필요한 컴포넌트는 가능한 경우 `[RequireComponent]`로 명시한다.
* 가능한 경우 `GetComponent`를 매 프레임 반복 호출하지 않는다.
* 반복 사용하는 Animator 파라미터는 `Animator.StringToHash`로 캐시한다.
* 런타임 코드에서 `UnityEditor` API를 참조하지 않는다.
* Editor 전용 코드는 반드시 `Assets/Editor/` 아래에 둔다.
* serialized 필드 이름 변경 시 기존 Inspector 데이터 보존을 위해 `FormerlySerializedAs` 사용을 검토한다.
* 게임 규칙을 애니메이션 이벤트에 의존시키지 않는다.
* 애니메이션은 게임 로직의 결과를 표현하는 역할만 맡긴다.
* 코드 수정 시 기존 기능이 깨지지 않는지 확인한다.
* 현재 프로젝트에 이미 존재하는 구조를 먼저 확인하고 중복 시스템을 만들지 않는다.

---

## 작업 방식

코드를 작성하기 전에 다음을 먼저 수행한다.

1. 관련 스크립트와 씬 구조를 확인한다.
2. 기존 클래스의 책임을 파악한다.
3. 변경할 파일 목록을 간단히 정리한다.
4. 현재 요청 범위를 벗어나는 기능은 구현하지 않는다.

코드를 작성할 때는 다음 규칙을 지킨다.

* 한 번에 전체 게임을 구현하지 않는다.
* 실행 가능한 작은 단위로 작업한다.
* 기존 코드를 수정할 때는 다른 클래스와의 연결을 확인한다.
* 새 시스템을 만들기 전에 기존 시스템으로 해결 가능한지 확인한다.
* 사용자가 요청하지 않은 대규모 리팩터링을 하지 않는다.
* 새로운 외부 패키지를 임의로 설치하지 않는다.
* 씬과 프리팹을 수정한 경우 변경 내용을 명확히 설명한다.

작업이 끝난 후 다음 내용을 보고한다.

* 생성한 파일
* 수정한 파일
* 구현한 기능
* Unity Inspector 설정 방법
* 테스트 방법
* 아직 구현하지 않은 부분
* 주의할 오류 가능성

---

## Unity 작업 지침

코드만 작성하고 끝내지 말고 Unity Editor에서 필요한 설정도 설명한다.

반드시 다음 내용을 구체적으로 알려준다.

* 어떤 GameObject를 생성해야 하는지
* 어떤 컴포넌트를 추가해야 하는지
* 어느 스크립트를 연결해야 하는지
* Inspector 필드에 무엇을 할당해야 하는지
* 태그나 레이어가 필요한지
* Tilemap 또는 Collider 설정이 필요한지
* 테스트할 때 어떤 키를 눌러야 하는지
* 정상 작동 시 무엇이 보여야 하는지

씬이나 프리팹을 직접 수정할 수 없는 경우 사용자가 따라 할 수 있도록 단계별로 설명한다.

---

## 테스트 규칙

각 개발 단계가 끝나면 최소한 다음을 확인한다.

### 이동 테스트

* 플레이어가 한 번 입력할 때 정확히 한 타일 이동하는가
* 입력이 없을 때 아무것도 움직이지 않는가
* 벽과 다른 캐릭터를 통과하지 않는가
* 타일 좌표와 화면 위치가 일치하는가

### 턴 테스트

* 플레이어 행동 전에는 적이 움직이지 않는가
* 플레이어 행동 한 번당 적이 정확히 한 번 행동하는가
* 하나의 적이 한 턴에 여러 번 행동하지 않는가
* 플레이어 입력이 적 턴 동안 차단되는가

### 투사체 테스트

* 플레이어 기본공격 투사체가 같은 행동 안에서 첫 대상 또는 맵 끝의 경계 벽까지 이동하는가
* 플레이어 기본공격 투사체가 경계 벽에 닿은 뒤 제거되는가
* 멀리 있는 적에게 적중해도 피해 처리 직후 투사체가 사라지고 대상을 관통하지 않는가
* 플레이어 기본공격의 비행과 피해 처리가 끝난 뒤 적 턴이 시작되는가
* 적·특수 투사체는 한 투사체 턴에 한 칸만 이동하는가
* 보드에 남는 투사체는 플레이어 입력이 없으면 정지하는가
* 벽 또는 대상과 충돌할 때 제거되는가
* 발사 후 목표를 임의로 추적하지 않는가

### 점유 테스트

* 두 캐릭터가 같은 타일에 들어가지 않는가
* 사망 후 타일 점유가 해제되는가
* 적 여러 마리가 같은 목적지로 이동하지 않는가

### 에셋 테스트

* 새 Sprite와 Aseprite 에셋이 32 PPU로 임포트되었는가
* 픽셀 아트가 Point 필터링으로 선명하게 표시되는가
* 몬스터 애니메이션이 독립 `.anim` 파일로 저장되었는가
* 몬스터 Animator Controller가 독립 `.controller` 파일로 저장되었는가
* 새 몬스터 Prefab이 생성되지 않았는가
* 이동하거나 생성한 모든 Unity 에셋에 대응하는 `.meta` 파일이 있는가

### Unity 프로젝트 검증

* Unity `6000.4.0f1`에서 스크립트 컴파일 오류가 없는가
* `SampleScene`이 참조 누락 없이 열리는가
* Unity Console에 이번 변경으로 새로 발생한 오류가 없는가
* 씬이나 에셋을 수정했다면 GUID와 Inspector 참조가 유지되는가

테스트를 실제로 실행할 수 없는 환경이라면 실행했다고 주장하지 않는다.

대신 필요한 테스트 절차와 예상 결과를 설명한다.

---

## 금지 사항

현재 단계에서는 다음 작업을 하지 않는다.

* 직업 선택 시스템 구현
* 동료 시스템 구현
* 최종보스 구현
* 엔딩 구현
* 복잡한 절차적 맵 생성
* 대각선 이동 구현
* 실시간 적 이동
* 판정을 프레임 시간이나 Transform의 실시간 이동에 의존시키는 투사체 구현
* 게임의 나머지 구성이 완성되기 전 유도탄 또는 추적형 특수공격 구현
* 32가 아닌 PPU로 새 Sprite 또는 Aseprite 임포트
* 몬스터 Prefab 신규 생성
* 몬스터 애니메이션을 Aseprite 내부 sub-asset으로만 보관
* 전체 시스템의 불필요한 재작성
* 요청하지 않은 외부 패키지 설치
* 사용자가 요청하지 않은 대규모 폴더 구조 변경

추후 기능을 고려한다는 이유로 현재 사용하지 않는 클래스와 코드를 대량으로 미리 만들지 않는다.

---

## 현재 최우선 목표

현재 최우선 목표는 다음 프로토타입을 안정적으로 완성하는 것이다.

플레이어가 작은 타일맵에서 이동 또는 공격 중 하나를 선택한다.

플레이어 기본공격은 같은 행동 안에서 전체 경로와 피해를 처리한다.

그 뒤 모든 적과 보드에 남는 적·특수 투사체가 각각 정확히 한 번씩 행동한다.

모든 적을 처치하면 강화 하나를 선택하고 다음 웨이브로 진행한다.

플레이어가 사망할 때까지 웨이브가 반복되고 최고 기록이 저장된다.

모든 구현 결정은 이 핵심 플레이 경험을 먼저 완성하는 방향으로 한다.
