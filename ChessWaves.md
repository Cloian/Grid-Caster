# SampleScene 체스 몬스터 웨이브

Unity 6000.4.0f1에서 `Assets/Scenes/SampleScene.unity`를 열고 Play → 시작 특성을 선택한다.
S는 이동, A는 공격 선택이며 주변 8방향 화살표 타일을 클릭한다. 기존 화면의 이동/공격 버튼도 사용할 수 있다.

| 웨이브 | 등장하는 체스 몬스터 |
|---|---|
| 1~2 | 없음, 기존 적 구성 |
| 3~4 | 나이트 1마리 |
| 5 이후 | 나이트·비숍·룩 각각 1마리 |

전체 적 수는 기존의 4마리 시작, 웨이브마다 1마리 증가를 유지한다. 체스 몬스터는 기존 적 일부를 대체한다.
기존 SampleScene 맵, 시작 특성, 필살기, 사망/묘비 UI를 유지한다. 전멸 시 기존처럼 체력 20%를 회복하고 다음 웨이브로 진행한다.
새 강화 선택 시스템은 추가하지 않았다. 테스트의 2회차·20턴·3분 제한 및 기록 입력 UI는 SampleScene에 붙이지 않는다.

나이트는 빈 타일로 L자 도약한 뒤 착지 지점의 상하좌우 1칸에 피해 1을 준다. 중심/대각선은 공격하지 않고 착지할 곳이 없으면 공격도 하지 않는다. 공격한 칸에는 다음 나이트 행동까지 `십자` 글자를 표시한다.
비숍은 매 턴 최대 2칸 대각선 이동하며 플레이어와 약 3칸 거리를 유지하면서 한 방향으로 맴도는 것을 선호한다. 벽/다른 적/플레이어를 통과하지 않고 가능한 경로가 없으면 쉰다. 출발점과 이동 경로/도착점에는 불길을 남긴다. 몸통 접촉/돌진 직접 피해는 없다.
불길은 다음 플레이어 행동 3회 동안 유지되며 진입하면 피해 1을 준다. 제자리에 서서 공격하는 것은 진입으로 보지 않는다. 겹친 불길은 피해를 중첩하지 않고 수명만 갱신한다. 입력 없이 시간이 흘러도 사라지지 않는다. `불길 3`처럼 남은 턴을 글자로 표시한다.
룩은 테두리의 도달 가능한 칸 중 플레이어에 가까운 곳을 향해 한 칸 이동하면서 매 턴 사격한다. 이동이 막히거나 이미 추적 위치에 도착해도 사격한다. 같은 행/열이면 플레이어 방향, 아니면 해당 변의 안쪽 직선으로 발사한다. 발사 후 유도하지 않고 새 탄/기존 탄 모두 투사체 단계에서 한 칸 진행한다. 넉백으로 테두리 밖에 밀려나지 않는다.
웨이브 종료 시 남은 적 탄과 불길을 정리한다. 하늘색 N, 노란색 B, 분홍색 R 표식과 위험 타일은 현재 플레이어 위치를 기준으로 한 예상이며 다음 플레이어 이동 후 적의 선택이 바뀔 수 있다.

## 설정

SampleScene에서 `MonsterSpawner`가 붙은 오브젝트의 `체스 몬스터 웨이브` 항목을 사용한다.
`Enable Chess Waves`와 Sprite 참조는 연결되어 있다. 체력과 나이트 행동 간격, `Bishop Move Distance`(2), `Bishop Orbit Distance`(3), `Bishop Fire Turns`(3), `Bishop Fire Damage`(1)는 테스트 씬에서 복사했다. 비숍 회복 턴과 룩 사격 간격은 새 행동에서 사용하지 않는다.
새 GameObject, 태그, 레이어, Collider 또는 몬스터 Prefab을 수동으로 만들 필요는 없다.
`ChessWaveDisplay`는 실행 시 스포너에 자동 연결되어 위험 칸과 이름/체력/바라보는 방향을 표시한다.
테스트 수치를 다시 복사하려면 Play 종료 후 `Tools > Playtest > Apply Chess Waves to SampleScene`을 실행한다.

## 변경 파일

- 생성: `Assets/Codes/ChessWaveDisplay.cs`, `Assets/Editor/ChessWaveSetupTool.cs`, 대응 `.meta`, 이 안내.
- 수정: `MonsterSpawner.cs` (해금 구성과 적→탄 순서, 웨이브 종료 탄 정리), `ChessMonsterBehaviour.cs` (스폰 방향 조준/룩 테두리 확인), `ChessPlaytestValidation.cs` (1~6웨이브 검증), `SampleScene.unity` (체스 웨이브 설정).
- 기존 `ChessPlaytest.unity`는 독립된 짧은 테스트로 유지한다.

검증 메뉴: `Tools > Playtest > Validate Chess Playtest`.
기본 패턴 테스트 후 SampleScene의 입력/전투와 1~6웨이브 해금, 총 마릿수, 룩 사격 및 웨이브 사이 탄 정리를 확인한다.
웨이브 진행 검증에서만 적을 강제로 처치하므로 실제 난이도·재미가 검증되었다는 의미는 아니다.

2026-09-11: Unity 6000.4.0f1 별도 복사본의 배치 Play에서 기본 패턴·입력·SampleScene 회귀·1~6웨이브 검증 통과 (`CHESS_PLAYTEST_VALIDATION_PASS`, 종료 코드 0). 검증된 씬을 원본에 반영했으며 SampleScene GUID를 유지했다.

추가 변경: `BishopFireTrail.cs`/`.meta` 생성. `ChessMonsterBehaviour.cs`, `MonsterMovement.cs`, `MonsterSpawner.cs`, `ChessPlaytest.cs`, `ChessWaveDisplay.cs`, `ChessWaveSetupTool.cs`, `ChessPlaytestValidation.cs` 및 두 씬 설정을 갱신했다. 새 이펙트 에셋은 만들지 않았다.

새 패턴 검증: Unity 6000.4.0f1 배치 Play 검증 통과, 종료 코드 0. 착지 후 십자 피해/대각선 무피해, 비숍 연속 대각선 순회와 경로 불길, 진입 피해·3턴 만료·중첩 방지, 룩 이동+사격·추적 방향 변경·탄 한 칸, 1~6웨이브 연결과 불길 정리를 확인했다. 두 씬의 GUID는 유지했다.

## 이동 경로 보완 (2026-09-13)

- 뱀/박쥐: 점유를 반영한 BFS 최단 경로 유지. 같은 길이면 목표 방향과 이전 진행 방향을 우선한다. 목표가 완전히 포위되면 거리를 줄일 수 있는 빈칸으로 접근하고, 더 접근할 수 없으면 대기한다. 박쥐는 8방향 이동/공격을 유지하지만 지형의 막힌 모서리를 통과하지 않는다.
- 예약 순서: 일반 적에도 고유 생성 순서를 부여해 같은 거리에서 처리 순서를 고정한다. 비숍의 중간 통과 칸도 한 턴 예약하고 출발 칸은 비워 준다.
- 나이트: 공격 가능한 십자 인접 착지 칸까지 L자 도약의 BFS 최단 경로를 찾는다. 바로 공격 가능한 착지는 우선한다. 공격 위치가 모두 막히면 가까워지는 도약만 허용한다.
- 비숍: 직전 칸 외에 최근 출발 칸 4개에도 재방문 페널티를 적용한다. 좁은 공간에서는 같은 칸으로 돌아올 수 있다.
- 룩: 테두리 경로에서 목표 거리와 경로 길이를 순서대로 비교한다. 동률인 경우에만 이전 이동 방향을 우선한다. 플레이어가 실제로 반대편으로 이동하면 추적 방향도 바뀐다.

이번 수정 파일은 `MonsterMovement.cs`, `MonsterSpawner.cs`, `ChessMonsterBehaviour.cs`, `ChessPlaytestValidation.cs`, 이 안내다. 새 파일이나 씬 변경, Inspector 추가 설정은 없다.

기존 검증 메뉴에 뱀의 포위된 목표 접근 후 대기, 장벽 우회 최단 12걸음, 상하좌우 한 칸 이동, 비숍 통과 칸 예약과 출발 칸 재사용, 웨이브 내 생성 순서 중복 검사를 추가했다.
직접 확인하려면 SampleScene Play → 시작 특성 선택 → S로 이동 선택 후 표시된 칸 클릭을 반복한다. 뱀은 빈 통로를 따라 접근하고, 장벽 뒤에서는 필요하면 잠시 멀어지는 방향으로 우회해야 정상이다. A 선택 후 방향 칸을 클릭하면 공격 한 번과 적 턴이 진행된다.

최단 경로는 각 적이 계획하는 시점의 점유 기준이다. 적 전체의 여러 턴 후 배치까지 최적화하는 협동 경로 계획은 포함하지 않는다. 길이 모두 막힌 대기, 필수 우회, 플레이어 이동에 따른 재탐색은 오류가 아니다. 이동 연출이 동시에 시작되므로 비운 출발 칸 주변에서 스프라이트가 잠깐 겹칠 수 있지만 목적지 점유는 중복되지 않는다.

검증 결과: Unity 6000.4.0f1 별도 복사본의 배치 Play에서 `CHESS_PLAYTEST_VALIDATION_PASS`, 종료 코드 0. 위 경로 검사와 박쥐 지형 모서리 검사, 기존 패턴/입력/1~6웨이브 회귀 검사를 통과했다. 로그: `/tmp/gridcaster-pathfinding-verified.log`. 실행 초기에 UnityEditor.Search.SearchDatabase 내부 인덱스 예외 1건이 있었으며 게임 코드 컴파일 오류는 없었다. 실제 마우스 조작의 체감과 모든 가능한 배치를 검증한 것은 아니다.
